package pdfsigner;

import pdfsigner.gui.GUI;

public class Main {
    public static void main(String[] args) {
        GUI gui = new GUI();
        gui.start();
        // Modifying a PDF
        // PdfWriter writer = new PdfWriter("C:/Users/1/Documents/Automaty vendingowe1_signed.pdf");
        // PdfReader reader = new PdfReader(outFile);
        // PdfDocument document = new PdfDocument(reader, writer);
        // writer.flush();
        // document.addNewPage(0);
        // document.close();
        // writer.close();
        // reader.close();
        return;
    }
}